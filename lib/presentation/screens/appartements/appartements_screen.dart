import 'package:flutter/material.dart';

class AppartementsScreen extends StatelessWidget {
  const AppartementsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Appartements')));
  }
}
