import 'package:flutter/material.dart';

class AgListScreen extends StatelessWidget {
  const AgListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('AG List')));
  }
}
