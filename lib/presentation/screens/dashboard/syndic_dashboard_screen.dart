import 'package:flutter/material.dart';

class SyndicDashboardScreen extends StatelessWidget {
  const SyndicDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Syndic Dashboard')));
  }
}
